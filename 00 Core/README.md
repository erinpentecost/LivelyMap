# Core

This data folder holds high-definition, classic-looking map textures.
It also contains the Lua scripts necessary for the mod to function.

The sync executable also places files into `scripts/LivelyMap/data/`, which are read by the Lua scripts in-game.
